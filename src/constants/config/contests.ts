const CONTEST_STATUSES = {
  OPEN: "open",
  MATCHED: "matched",
  UNMATCHED: "unmatched",
  CLOSED: "closed",
};

const LEAGUE_CONTEST_STATUSES = {
  OPEN: "open",
  MATCHED: "matched",
  UNMATCHED: "unmatched",
  CLOSED: "closed",
};

export { CONTEST_STATUSES, LEAGUE_CONTEST_STATUSES };
